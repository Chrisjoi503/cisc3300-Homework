CISC 3300 F24 
In class assignment 2 
1. An __span____ element does not start on a new line. 
2. Image files can be accessed __interally relatively or absolutely methods using the source (src) element that follows a path or externally using links____ to the html file. 
3. The __compression____ algorithm for a PNG file is lossless. 
4. The ____form__ element gives you a set of input elements to collect data from your users.
 5. CSS makes __styles____ rules for html elements. 
6. More specific __selectors____ (such as using an ID instead of a class) will take precedence over less specific __selectors____. 
7. HTML treats every element as living in its own __box____. 
8. Specifying auto as the second parameter of margin basically tells the browser to automatically determine the left and right margins itself, which it does by setting them __equally ____. 
9. The __padding____ property allows you to specify how much space should appear between the content of an element and its border. 

